# HANDOFF

规则：只追加，不回写历史。
模板：`已完成 / 未完成 / 阻塞 / 下一步`

---
## 2026-03-02 交接记录 #001

### 已完成
1. 已将新站目录从 `web_1/` 迁移为 `web/`。
2. 已删除弃用旧站目录 `website/`。
3. 已在 `web/docs/` 重建并重写 `PROJECT.md`、`TASKS.md`、`HANDOFF.md`。
4. 旧 `web/` 文档已备份到 `/tmp/va-web-docs-backup-20260302-131929`。

### 未完成
1. 首发范围要求的 `/products/[slug]` 与 `/contact` 路由尚未落地。
2. 首页和产品页真实素材与对外文案尚未替换到可发布状态。
3. 关键参数确认项（`PC-003/004/006/010`）尚未关闭。

### 阻塞
1. 技术参数口径需刘工确认后，详情页参数才能定稿。
2. 品牌对外口径（名称/邮箱/文案）仍有模板残留，需统一确认。

### 下一步
1. 先补齐 `/products/[slug]` 与 `/contact`，达成首发 5 页完整范围。
2. 并行替换首页/产品页真实素材，清理模板品牌残留。
3. 参数确认关闭后，执行上线前 QA 与 Vercel 发布。
