# HANDOFF

规则：只追加，不回写历史。
模板：`已完成 / 未完成 / 阻塞 / 下一步`

---
## 2026-03-02 交接记录 #001

### 已完成
1. 已将新站目录从 `web_1/` 迁移为 `web/`。
2. 已删除弃用旧站目录 `website/`。
3. 已在 `web/docs/` 重建并重写 `PROJECT.md`、`TASKS.md`、`HANDOFF.md`。
4. 旧 `web/` 文档已备份到 `/tmp/va-web-docs-backup-20260302-131929`。

### 未完成
1. 首发范围要求的 `/products/[slug]` 与 `/contact` 路由尚未落地。
2. 首页和产品页真实素材与对外文案尚未替换到可发布状态。
3. 关键参数确认项（`PC-003/004/006/010`）尚未关闭。

### 阻塞
1. 技术参数口径需刘工确认后，详情页参数才能定稿。
2. 品牌对外口径（名称/邮箱/文案）仍有模板残留，需统一确认。

### 下一步
1. 先补齐 `/products/[slug]` 与 `/contact`，达成首发 5 页完整范围。
2. 并行替换首页/产品页真实素材，清理模板品牌残留。
3. 参数确认关闭后，执行上线前 QA 与 Vercel 发布。

---
## 2026-03-02 交接记录 #002

### 已完成
1. 已落地 `/contact` 页面（前端占位提交流程）。
2. 已落地 `/products/[slug]` 页面（Product Detail）并打通列表到详情入口。
3. 已落地 `/products/[slug]/datasheet` 页面并打通详情到 Datasheet 入口。
4. 已完成 Detail/Datasheet 图片分层策略：Detail 显示 1/4 类，Datasheet 显示 2/3 类。
5. 已统一导航与 CTA 路径，Contact 统一指向 `/contact`，并完成品牌口径清理（VA）。

### 未完成
1. Datasheet 第 2/3 类技术真图仍未全部补齐。
2. Datasheet PDF 下载仍为占位状态（Coming Soon）。
3. 参数确认项 `PC-003/004/006/010` 仍为 Open。

### 阻塞
1. 参数口径需刘工确认后，技术参数才可标记“可发布”。
2. 素材侧缺少完整 close-up 与 dimension 标注图，影响技术页说服力。
3. 本地构建验证仍受外网字体拉取和依赖环境影响。

### 下一步
1. 补齐 2/3 类技术真图并替换占位卡。
2. 接入正式 Datasheet PDF 下载文件。
3. 关闭关键参数确认项后执行发布前 QA 与 Production 发布。

---
## 2026-03-06 交接记录 #003

### 已完成
1. 已根据 2026-03-06 与刘工沟通结果，同步“3 主线 + 2 配套”产品范围到文档。
2. 已更新 `ASSETS_INDEX.md`：新增已确认产品线口径区块，并将 `A-027` 标记为可用。
3. 已更新 `PENDING_CONFIRMATIONS.md`：关闭 `PC-001`、`PC-010`，新增 `PC-017/018/019`。
4. 已更新 `web/docs/PROJECT.md` 与 `TASKS.md`：同步当前产品范围与新增资料跟进任务。
5. 已更新根目录 `OVERVIEW.md` 与 `ACTION_BOARD.md`：统一为当前阶段节奏。

### 未完成
1. `PC-003/004/006` 参数字段口径仍未关闭。
2. `PC-011/012/013/016`（品牌/映射/标准口径）仍待拍板。
3. `PC-017/018/019`（配套产品与新产品资料）仍待交付计划。

### 阻塞
1. Datasheet 技术图与 PDF 仍不完整，影响技术页可信度闭环。
2. 配套产品资料尚未提供可发布版本，影响后续产品扩展上线。

### 下一步
1. 先关闭 `PC-003/004/006/012`，把型号与参数映射固定下来。
2. 并行推进快锁式带电线夹、电动安装工具资料交付计划（`PC-017/018`）。
3. 完成 Datasheet 真图与 PDF 接入后执行发布前 QA。

---
## 2026-03-22 交接记录 #004

### 已完成
1. 已归档新收到的中英文 3 页 brochure：`A-029`（中文）与 `A-030`（英文）。
2. 已确认二者属于同一套 corporate/product brochure 的成对版本，而非独立散页。
3. 已更新 `materials/README.md`、`materials/MANIFEST.csv`、`ASSETS_INDEX.md`，补充来源路径、SHA256 和用途。
4. 已在 `PENDING_CONFIRMATIONS.md` 新增 `PC-020/021/022`，记录 brochure 暴露出的认证年份、型号前缀、强结论/项目背书风险。

### 未完成
1. 新 brochure 仍未达到“可直接对外发布”状态。
2. `JCX/JZC/JCK` 映射、品牌主体口径、ISO 年份与强结论出处仍未确认。

### 阻塞
1. brochure 复用了部分旧资料口径，受 `PC-011/012/013/016` 影响。
2. 中文与英文稿为成对版本，但不是严格逐句等值翻译，后续英文润色仍有空间。

### 下一步
1. 先关闭 `PC-020/021/022`，决定新 brochure 哪些表述可以继续保留。
2. 若用于外发，需统一品牌、认证和型号前缀后再做一轮文案润色。
